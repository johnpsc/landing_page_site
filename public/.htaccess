RewriteEngine On

# Se o arquivo ou diretório real existir, serve normalmente

RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Caso contrário, redireciona para index.html (SPA fallback)

RewriteRule ^ index.html [QSA,L]
